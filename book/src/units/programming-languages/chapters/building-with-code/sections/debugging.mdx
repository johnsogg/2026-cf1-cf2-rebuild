export const title = "Debugging"

_This section is a placeholder._

{/*
  Finding and fixing things that are broken — a skill, not an accident. Goals:
    - Reading error messages: what the browser console is and how to open it
    - Syntax errors vs. runtime errors vs. logic errors
    - console.log as the primary debugging tool: logging variable values
      at key moments to understand what the program is actually doing
    - The debugging process as a hypothesis/experiment cycle: "I think X is
      wrong, let me check"
    - Common p5.js beginner bugs: undefined variable, off-by-one in loops,
      forgetting to call background() or calling it too late, coordinate
      system confusion
  The goal is to give students a reliable process rather than a collection
  of specific fixes, so they can debug problems they haven't seen before.
*/}
