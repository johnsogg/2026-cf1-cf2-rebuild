// @ts-nocheck
export const greet = (name: string): string => {
  // IMPLEMENT ME
  // Note: don't print out the name. Return a string instead.
}

export const square = (n: number): number => {
  // IMPLEMENT ME
}
