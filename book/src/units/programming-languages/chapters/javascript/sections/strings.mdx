export const title = "Strings"

_This section is a placeholder._

{/*
  Working with text as data. Goals:
    - String literals, template literals (`${...}`) for building text from
      values — already used informally in earlier sections, formalize it here
    - Common string properties/methods: length, indexOf, includes, slice,
      toUpperCase/toLowerCase, split, trim
    - Concatenation vs. template literals — when to reach for each
    - Strings as sequences of characters (indexing, iteration) as a bridge
      toward the Lists section
  Practical context: labeling a sketch, building a message from live values
  (score, timer, coordinates), parsing simple user-typed input.
*/}
