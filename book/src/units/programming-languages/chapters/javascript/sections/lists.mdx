export const title = "Lists"

_This section is a placeholder._

> Collections of values — the foundation of every interesting program. Goals:
>
> - Array syntax: [], indexing with [], .length
> - Creating arrays: literal, push(), and initialized with a loop
> - Iterating with for loops and forEach
> - Common patterns: draw-everything loop, update-everything loop,
>   remove-dead-things loop (filter or splice)
> - Arrays of objects (connecting back to the Objects section) Practical
>   context: a sketch with many independently-moving particles, a simple "paint"
>   program that records strokes, collecting mouse positions to draw a trail.
>   The key insight is that you can have an arbitrary number of things without
>   writing an arbitrary amount of code.
>
> Revisit the animated planets example one more time, this time forming a list
> for an arbitrary number of planets. Increase particle count to six to show
> that the code itself is shorter, while seemingly having more going on in the
> graphics.
