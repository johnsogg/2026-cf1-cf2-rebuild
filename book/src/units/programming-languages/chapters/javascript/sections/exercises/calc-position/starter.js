// position and velocity are p5 vectors (like the ones createVector makes).
// Each one has an .x and a .y. calcPosition should return a NEW vector for
// where the object is after one step of motion: position + velocity.
// Leave the original position and velocity vectors unchanged.
export function calcPosition(position, velocity) {
  // IMPLEMENT ME
}
