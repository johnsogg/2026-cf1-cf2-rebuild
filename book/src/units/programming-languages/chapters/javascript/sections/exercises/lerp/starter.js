// Write your own version of map(), called lerp (short for "linear
// interpolation"). Given a value v somewhere in a source range, return the
// equivalent value in a target range.
export function lerp(v, source_low, source_high, target_low, target_high) {
  // IMPLEMENT ME
}
