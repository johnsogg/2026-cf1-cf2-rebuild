// Treat x and y as the two parts of a vector. magnitude(x, y) should return
// the vector's length — the distance from the origin (0, 0) to the point (x, y).
export function magnitude(x, y) {
  // IMPLEMENT ME
}
