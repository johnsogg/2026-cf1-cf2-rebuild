export const title = "Conditionals"

_This section is a placeholder._

{/*
  Teaching the program to make decisions. Goals:
    - if / else if / else syntax
    - Boolean expressions: comparison operators (===, !==, <, >, <=, >=)
    - Logical operators: && (and), || (or), ! (not)
    - Nested conditionals
    - Common patterns: checking if something is inside a region, toggling a
      flag on/off, choosing between behaviors based on state
  Practical context: making things happen when the mouse is in a certain area,
  changing color/shape based on a condition, responding differently to different
  keys. Pairs naturally with the interactivity section.
*/}
