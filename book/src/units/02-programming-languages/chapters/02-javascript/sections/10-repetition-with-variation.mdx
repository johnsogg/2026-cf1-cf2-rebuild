export const title = "Repetition with Variation"

_This section is a placeholder._

{/*
  Loops as a creative tool — using systematic variation to make visually
  rich outputs. Goals:
    - Using the loop counter to vary more than just position
      (size, color, opacity, rotation, spacing)
    - random() for unpredictable variation — and how randomSeed() makes
      randomness reproducible
    - noise() (Perlin noise) for organic-feeling variation
    - map() for translating a counter range into a useful value range
    - The distinction between "repetition" (sameness) and "repetition
      with variation" (pattern and rhythm)
  Practical context: a field of flowers that vary in size and color, a
  skyline where buildings have different heights, a generative texture.
  This is where coding starts to feel like a design tool rather than a
  math exercise.
*/}
