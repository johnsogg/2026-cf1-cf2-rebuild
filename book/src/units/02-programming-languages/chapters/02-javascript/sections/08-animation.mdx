export const title = "Animation"

_This section is a placeholder._

{/*
  Making things move over time. Goals:
    - Two ways to mark the passage of time in p5.js:
        1. frameCount — an integer that increments each draw() call
        2. millis() — wall-clock milliseconds since the sketch started
    - Using time to drive position, size, color, rotation
    - The pattern: compute a property as a function of time (e.g., sin/cos
      for oscillation, linear interpolation for smooth movement)
    - Storing state across frames with sketch-level variables
    - background() at the top of draw() to clear the previous frame
      (vs. intentionally not clearing for trail effects)
  Practical context: bouncing ball, sunrise/sunset, breathing circle —
  any change that unfolds over time without user input.
*/}
