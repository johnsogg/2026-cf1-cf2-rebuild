export const title = "Text"

_This section is a placeholder._

{/*
  Using text as a visual element in a sketch. Goals:
    - text(str, x, y) for drawing a string
    - textSize(), textAlign(), textStyle()
    - loadFont() and preload() for custom fonts (p5.Font, opentype)
    - Measuring text: textWidth(), textAscent(), textDescent() — useful
      for centering or flowing text
    - Animating text: typing effect (revealing characters one by one using
      frameCount or millis), oscillating position or size, per-character
      transforms by splitting a string into an array of characters
  Text is treated as a visual/graphical element here, not as UI. The goal
  is for students to see strings as material to manipulate and display,
  not just as debugging output.
*/}
