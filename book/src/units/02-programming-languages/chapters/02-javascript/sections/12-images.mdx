export const title = "Images"

_This section is a placeholder._

{/*
  Loading and displaying image assets in a sketch. Goals:
    - loadImage() and preload() — why preload exists (async asset loading)
    - image(img, x, y) and image(img, x, y, w, h) for drawing
    - imageMode(CENTER) vs. default corner mode
    - Swapping between images (storing multiple loaded images, switching
      which one is drawn based on state)
    - tint() to colorize or fade an image
    - pixels[] for direct pixel manipulation (optional/advanced)
  Browser-specific nuance: images must be loaded from a server or a
  data URL due to CORS; the p5.js web editor handles this transparently
  but students working locally need to know why file:// paths don't work.
*/}
