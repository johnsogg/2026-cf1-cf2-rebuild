export const title = "Functions"

_This section is a placeholder._

{/*
  Giving names to chunks of behavior so they can be reused. Goals:
    - Defining a function with function keyword (and arrow function variant)
    - Parameters and arguments
    - Return values
    - Variable scope: local vs. sketch-level
    - Why functions matter: avoiding repetition, making code readable,
      isolating a concern so it can be reasoned about independently
  Practical context: writing a drawHouse(x, y, size) or drawCloud(x, y)
  function and calling it several times — shows immediate payoff of
  encapsulation. Connects to p5.js's setup/draw model, which students have
  been using without fully understanding that they're defining functions.
*/}
