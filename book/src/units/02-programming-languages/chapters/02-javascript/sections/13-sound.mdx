export const title = "Sound"

_This section is a placeholder._

{/*
  Loading and playing audio — with special attention to browser constraints. Goals:
    - p5.sound library: loadSound() and preload()
    - play(), pause(), stop(), loop()
    - Checking isPlaying() to avoid double-play
    - volume() and rate() for basic control
    - The browser autoplay policy: browsers block audio that starts before
      the user has interacted with the page. This means sound must be
      triggered from a user gesture (mousePressed, keyPressed) — not from
      setup() or automatically on load
    - A reliable pattern: show a "tap to start" overlay, start audio in
      the handler, then remove the overlay
  This is the most common source of confusion for students new to web
  programming. The explanation should normalize the frustration and give
  a clear, copy-pasteable pattern for handling it.
*/}
