export const title = "Objects"

_This section is a placeholder._

{/*
  Grouping related values under a single name. Goals:
    - Object literals: { x: 10, y: 20, radius: 5, color: 'red' }
    - Reading and writing properties with dot notation
    - Passing objects to functions
    - Arrays of objects (natural bridge to the Lists section)
    - Why objects help: instead of parallel arrays (xs[], ys[], radii[]),
      one array of objects keeps related data together
  This section stays at the level of plain data objects (no classes yet).
  The motivating example is a "thing" in the sketch — a ball, a particle,
  a character — that has multiple properties that need to travel together.
  Classes and constructors are out of scope here; the concept is "a bundle
  of named values."
*/}
