export const title = "Loops"

_This section is a placeholder._

{/*
  Running the same code many times without typing it many times. Goals:
    - for loop: initialization, condition, increment
    - while loop (and when to prefer each)
    - Loop variable as a counter and as a value fed into drawing commands
    - Nested loops for grids
    - Off-by-one errors and how to think about them
  Practical context: drawing a row of circles, a grid of rectangles, a
  repeating pattern. The city scene assignment naturally motivates loops
  (many windows, many buildings, many people). Pairs with the functions
  section — call a function inside a loop.
*/}
