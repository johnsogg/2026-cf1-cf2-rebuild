export const title = "Tooling"

_This section is a placeholder._

{/*
  A cursory introduction to the ecosystem around code. Goals:
    - console.log for inspecting data structures — logging objects and arrays
      and reading them in the browser console
    - Copying JSON output from the console and using it in other programs
      (e.g., saving a color palette generated in a sketch and bringing it
      into another tool)
    - A brief tour of what "tooling" means: editors, terminals, package
      managers, version control — presented as orientation rather than
      instruction (these topics get full treatment later)
  This is intentionally surface-level. The goal is awareness — students
  should know these tools exist and have a mental map of what each one
  does before they encounter them in depth.
*/}
