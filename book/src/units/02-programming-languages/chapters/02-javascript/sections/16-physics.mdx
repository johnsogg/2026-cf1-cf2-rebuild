export const title = "Physics"

_This section is a placeholder._

{/*
  Using math to make things move in physically plausible ways. Goals:
    - Vectors as a way to bundle x and y into one named thing:
      { x, y } for position, velocity, acceleration
    - The update loop: position += velocity * dt, velocity += acceleration * dt
    - Using millis() or a fixed timestep to get dt (delta time)
    - Gravity as a constant downward acceleration
    - Damping / friction as scaling velocity each frame
    - Bouncing off walls: negate the relevant velocity component,
      optionally multiply by a restitution coefficient < 1
    - p5.Vector as an alternative to plain objects (add, mult, mag, normalize)
  This section is intentionally conceptual — the goal is for students to
  understand position/velocity/acceleration as a pattern, not to implement
  a full physics engine. A bouncing ball with gravity and wall bouncing is
  the canonical worked example.
*/}
