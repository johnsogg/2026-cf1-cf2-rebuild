export const title = "Gray Dot"

# Gray Dot

Here:

```p5sketch autoplay width=150 height=150
function setup() {
  createCanvas(150, 150)
}

function draw() {
  background("black")
  fill("#888")
  circle(75, 75, 45)
}
```

That's the output a program. It looks like it is an image, and you're not wrong.
But there is no image file, no png or jpg. That gray dot on a black background
is the result of a little program running in your browser. The output is an
image, and you can right-click to save it. The difference is how it got to you.

Here's another program that changes if you move your mouse over it:

```p5sketch autoplay width=150 height=150
function setup() {
  createCanvas(150, 150)
}

function draw() {
  background("black")
  if (45 < mouseX && mouseX < 105 && 45 < mouseY && mouseY < 105) {
    fill("#7AB8D4")
  } else {
    fill("#888")
  }
  circle(75, 75, 45)
}
```

It is just a little more complex than the first one. It checks to see if your
mouse pointer is near the circle, and changes its color if is. Obviously, these
are both super simple, and you've seen mouse hover effects a zillion times. But
how often have you stopped to think: _how does this actually work?_

Throughout this book, we're going to work towards answering that question.
You'll have the chance to build little hacks like this, and my hope is that
you'll start to build an intuition of what is possible with code, how to write
it, and how to think of it as a creative tool.

Here's another one. Before reading on, get a feel for what it is doing. Try to
explain to yourself what you _think_ it is doing before reading on.

Tell me: _what do you see?_

```p5sketch autoplay width=150 height=150
const colors = [
  "#7AB8D4",
  "#C4621D",
  "#E8C84A",
  "#C8D5B0",
  "#8B5E3C",
  "#F0EBE0",
]

let idx = 0
let inside = false

function setup() {
  createCanvas(150, 150)
}

function draw() {
  background("black")
  if (45 < mouseX && mouseX < 105 && 45 < mouseY && mouseY < 105) {
    if (!inside) {
      fill(colors[idx])
      idx = (idx + 1) % colors.length
      inside = true
    }
  } else {
    fill("#888")
    inside = false
  }
  circle(75, 75, 45)
}
```

Did you figure out what it is doing? Maybe you don't know _how_ it does it, not
yet at least. Throughout this book, I'll ask you to observe and externalize what
you experience. This is an important skill! Learning to create demands that you
not simply be a passive consumer of the flashy pixels, but to be able to
translate ideas and observations into a workable format. In this book, that
format is code.

Here's how I would describe the little program above: whenever the mouse moves
near the circle, it changes color and stays that color until the mouse moves
away and back again. It rotates through a list of six colors.

What did I miss? (I missed something. I always miss something.)
